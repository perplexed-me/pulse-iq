# Dependencies
node_modules
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json.backup

# Explicitly include package-lock.json (ensure it's not accidentally excluded)
!package-lock.json

# Build outputs (exclude from context for faster uploads)
dist
build
.vite

# Development files that shouldn't be in production builds
.env.local
.env.development.local
.env.test.local
.env.production.local
*.log

# IDE and editor files
.vscode
.idea
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db

# Git files
.git
.gitignore

# Documentation (not needed in container)
README.md
*.md
!package.json

# Backup files
*.backup

# Docker files (not needed inside container)
Dockerfile.*
docker-compose.*
.dockerignore

# CI/CD files
.github
.gitlab-ci.yml

# Cache directories (can be large and unnecessary)
.npm
.eslintcache
.cache

# Test files (not needed in production)
**/*.test.*
**/*.spec.*
coverage/

# Source maps (if not needed in production)
*.map
